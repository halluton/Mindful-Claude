{
  "description": "mindful-claude: guided breathing above the prompt while Claude works. Needs CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1; builds without function hooks ignore the modules key.",
  "modules": ["./register.tsx"]
}
